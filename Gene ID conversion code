if (!requireNamespace("BiocManager", quietly = TRUE))
  install.packages("BiocManager")

BiocManager::install("AnnotationDbi", force = TRUE)
 
browseVignettes("AnnotationDbi")

R_4$Geneid = mapIds(org.Hs.eg.db,
                    keys=as.list(R_4$Geneid), #Column containing Ensembl gene ids
                     column="ENTREZID",
                     keytype="ENSEMBL2EG",
                     multiVals="first")


if (!requireNamespace("BiocManager", quietly = TRUE))
  install.packages("BiocManager")

BiocManager::install("org.Hs.eg.db", force = TRUE)



browseVignettes("org.Hs.eg.db")

R_1conv<-as.list(R_1$Geneid)
R_1conv<-as.list(org.Hs.egALIAS2EG)


if(length(R_4conv) > 0){
  # Gets the entrez gene IDs for the first five Ensembl IDs
  xx[1:5]
}
